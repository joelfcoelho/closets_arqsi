import { NameSearchFilterPipe } from './nameSearchFilter.pipe';

describe('NameSearcFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NameSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
